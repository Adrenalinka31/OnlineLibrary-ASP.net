namespace OnlineLibrary.Domain.Enum
{
    public enum StatusCode
    {
        //User
        UserNotFound = 0,
        BookNotFound = 10,
        OK = 200,
        InternalServerError = 500,

    }
}
